[project]
name = "invenio-ccmm"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "xmltodict",
    "rich-click",
    "ruamel.yaml"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
